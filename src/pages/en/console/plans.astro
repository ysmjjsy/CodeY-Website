---
import CloudAdmin from '../../../components/CloudAdmin.astro'
import ConsoleShell from '../../../components/ConsoleShell.astro'
import { cloudCopy } from '../../../i18n/cloud'
import MarketLayout from '../../../layouts/MarketLayout.astro'

const locale = 'en' as const
const copy = cloudCopy(locale)
---

<MarketLayout title={`${copy.planAdminTitle} · CodeY`} description={copy.planAdminDescription} locale={locale} alternateHref="/console/plans/">
  <ConsoleShell locale={locale} activeSection="plans">
    <CloudAdmin locale={locale} section="plans" />
  </ConsoleShell>
</MarketLayout>
